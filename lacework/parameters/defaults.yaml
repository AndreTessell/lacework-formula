# -*- coding: utf-8 -*-
# vim: ft=yaml
#
# Set default values.
---
values:
  pkg:
    name: lacework
  rootgroup: root
  config: '/etc/lacework'
  service:
    name: lacework
  subcomponent:
    config: '/etc/lacework-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
...
